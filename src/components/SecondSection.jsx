import React from "react";
import styled from "styled-components";

const SecondSection = () => {
  return <ParentContainer>SecondSection</ParentContainer>;
};

export default SecondSection;

const ParentContainer = styled.section`
  display: flex;
  position: relative;
`;
